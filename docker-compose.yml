version: '3'

services:
  backend:
    build: backend
    volumes:
      - ./backend:/backend
    environment: 
      - FLASK_APP=app.py
      - FLASK_ENV=development
    expose:
      - '5000'
    ports:
      - '5000:5000'

  reactfrontend:
    build: reactfrontend
    volumes:
      - ./reactfrontend:/reactfrontend/src
      - ./reactfrontend:/reactfrontend/public
    environment:
      - NODE_ENV=development
    expose:
      - '3000'
    ports:
      - '3000:3000'
volumes:
  node-modules: